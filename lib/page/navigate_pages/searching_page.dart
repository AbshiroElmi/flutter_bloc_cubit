import 'package:flutter/material.dart';

class Searching_page extends StatelessWidget {
  const Searching_page({super.key});

  @override
  Widget build(BuildContext context) {
        return Container(child: Center(child: Text('Searching_pag')),);
  }
}