import 'package:flutter/material.dart';

class Bar_page extends StatelessWidget {
  const Bar_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('Bar_page')),);  }
}